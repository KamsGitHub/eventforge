# Not yet read by application code (config loading arrives in Milestone 3).
# Documented here so the shape of required env is visible from the start.

NODE_ENV=development
PORT=3000
LOG_LEVEL=info

DATABASE_URL=postgresql://eventforge:eventforge@localhost:5432/eventforge

KAFKA_BROKERS=localhost:9092
KAFKA_CLIENT_ID=eventforge
